import React from 'react';
import Search from './Search';

const ListHeader = () => {
  return (
    <div>
      <button>레이블</button>
      <button>마일스톤</button>
      <button>글쓰기</button>
    </div>
  );
};

export default ListHeader;
