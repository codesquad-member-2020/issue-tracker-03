spring:
  profiles:
    active: local
  flyway:
    clean-on-validation-error: true
management:
  endpoints:
    web:
      exposure:
        exclude: env,beans
        include: "*"
---
spring:
  profiles: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/issue?useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: root
  flyway:
    user: root
    password: root
    url: jdbc:mysql://localhost:3306/issue?useUnicode=yes&characterEncoding=UTF-8

  jpa:
    database-platform: org.hibernate.dialect.MySQL57InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: validate

logging:
  level:
    web: debug
    sql: debug
    com.codesquad.issue: debug

github:
  uri: https://github.com/login/oauth/access_token
  clientId: b8e8adfa2d70cc8d1240
  clientSecret: b3cc119bec1adb3b95e72e4b3adea08a5d83d083

host: http://127.0.0.1:8080
